/*
package frc.robot.commands;

import edu.wpi.first.networktables.NetworkTable;
import edu.wpi.first.networktables.NetworkTableEntry;
import edu.wpi.first.networktables.NetworkTableInstance;


written by dom``    A
public class VisionDriver {
    public static void PhotonVisionCam(){
        final NetworkTable table = NetworkTableInstance.getDefault().getTable("photonvision");
        
        double LedMode = table.getEntry("LedMode").getDouble(0.0);
        if(EngageDriver)
        
    }
    public static void EngageDriver(){
        int onOff = (1); 
    }
}
*/